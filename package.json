{
  "name": "aktyor-az",
  "version": "0.1.0",
  "private": true,
  "description": "Aktyor.az actor database and digital verification cards",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3010",
    "dev:api": "npm run dev --prefix backend",
    "start:api": "npm run start --prefix backend",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3010"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.17",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.9.2",
    "vite": "^6.0.7"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
